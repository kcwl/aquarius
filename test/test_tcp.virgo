enum color
{
	red;
	yellow;
	green;
}

struct tcp_person
{
	bool sex;
	uint32 addr;
	int32 age;
	uint64 telephone;
	int64 score;
	float hp;
	double mana;
	string name;
	bytes orders;
}

message login -> 10001
{
	request
	{
		tcp_person per_req;
	}
	response
	{
		tcp_person per_resp;
	}
}