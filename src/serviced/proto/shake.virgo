message shake -> /shake
{
	request
	{
		string host;
		int32 port;
		string name;
		string group;
		sint32 weight;
		string version;
		repeated string topics;
	}
	response
	{
		repeated string topics;
	}
}